import React from 'react';
import { NavLink } from 'react-router-dom';

const Header = (): JSX.Element => (
  <div className="main">
    <NavLink to="/">MAIN pages</NavLink>
  </div>
);

export default Header;
